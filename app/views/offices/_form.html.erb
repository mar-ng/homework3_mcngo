<%= form_with(model: office, local: true) do |form| %>
  <% if office.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(office.errors.count, "error") %> prohibited this office from being saved:</h2>

      <ul>
        <% office.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :office_street_address %>
    <%= form.text_field :office_street_address %>
  </div>

  <div class="field">
    <%= form.label :office_zip %>
    <%= form.number_field :office_zip %>
  </div>

  <div class="field">
    <%= form.label :office_phone %>
    <%= form.number_field :office_phone %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
